import React from 'react';
import { BrowserRouter } from 'react-router-dom';
import LectureApp from '../LectureApp';

const Root = () => (
    <BrowserRouter>
        <LectureApp />
    </BrowserRouter>
)

export default Root;